export const DB_NAME = "videotube"
// It is the just name of application's database not a system specific variable. Therefore it's not placed in .env file.
// It can be placed in .env file. But it's not that much sensitive.